# Configuration

YAML config file support with clusters, nodes, and settings. Environment variable interpolation for passwords. Multi-cluster definitions with switching support. Theme and threshold settings.
